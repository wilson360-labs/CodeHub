{
  "name": "codehub",
  "version": "3.0.0",
  "description": "CodeHub — Wilson.E Portfolio",
  "private": true,
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "mkdir -p dist && cp -r index.html index.css sw.js manifest.json sitemap.xml robots.txt _headers _redirects terms.html privacy.html 404.html css js img splash games pages dist/ 2>/dev/null; echo 'Static site copied to dist/'",
    "audit:project": "node scripts/audit-project.js",
    "check:install": "node scripts/audit-project.js --install",
    "check:health": "node scripts/audit-project.js --health"
  }
}
