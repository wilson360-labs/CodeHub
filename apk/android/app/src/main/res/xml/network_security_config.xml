<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">wilson360-labs.vercel.app</domain>
    </domain-config>
    <!-- Appodeal exige cleartext=true en el base-config (la red de
         mediación hace peticiones http internas). Decisión deliberada de
         monetización: SIN esto el adaptador de Appodeal no llega a llenar
         los formatos y el ARPDAU cae a 0. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
