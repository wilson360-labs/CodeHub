buildscript {
    ext {
        compileSdk = 34
        minSdk = 24
        targetSdk = 34
        versionCode = 3
        versionName = "1.2.0"
    }
}

plugins {
    // AGP 8.9.2 — requisito mínimo para compileSdk 36 (Appodeal adapters).
    // Requiere Gradle 8.11.1+ (ver .github/workflows/build-apk.yml).
    id 'com.android.application' version '8.9.2' apply false
    id 'com.google.gms.google-services' version '4.5.0' apply false
    id 'org.jetbrains.kotlin.android' version '2.3.21' apply false
}
